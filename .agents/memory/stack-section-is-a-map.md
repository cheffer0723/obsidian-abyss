---
name: "THE FULL STACK" (#stack) is a thin nav map, not detailed cards
description: obsidian-abyss home — why PlatformCards is deliberately compact and must not re-explain other sections
---

`PlatformCards` (`section#stack`, heading "THE FULL STACK") is intentionally a
compact 5-tile overview MAP (icon + eyebrow + title + one-line role + anchor),
NOT paragraph cards.

**Why:** every concept it references is detailed in a dedicated section, so full
descriptions there are pure duplication. The user explicitly asked to "tighten it,
drop anything duplicated." Mapping:
- Cerberus / Charon / Flow  → `#platform` (Modules "THE ARSENAL": Orthrus/Hydra/Sisyphus, Charon, Flow, Backtest, Non-Custodial)
- MiroFish                  → `#mirofish` (MiroFish "SIMULATE THE CROWD")
- x402                      → `#agents`   (Agents "PAY PER SIGNAL", 402 native)
- (a former "Proof" card was dropped — the interactive Backtest panel `#proof` sits directly above this section)

**How to apply:** if asked to expand the stack, add tiles/links — do NOT paste back
full explanatory copy that re-states those sections. Keep it a map.
